# 움직이는 자동차

자동차는 이름을 가지고 있으며, 움직일 수 있다. 

## Car 클래스
move: 0에서 9 사이에서 random 값을 구한 후 random 값이 4 이상일 경우 전진하고, 3 이하의 값이면 멈춘다.
addDistance(int value): value 만큼 이동 거리를 증가시킨다.
getDistance(): 이동 거리를 반환한다.
getName(): 자동차 이름을 반환한다.

